- Add wiring diagram showing the (very simple) wiring set up
- Read Pi serial number from /proc/cpuinfo at runtime rather than statically defining it before compilation
- Fix for detection of DOUBLE/TRIPLE/DOUBLE LONG/TRIPLE LONG click patterns (only DOUBLE is actually required for emulation). Build buttontest.cpp to see the desired behaviour
